import { View, Text } from "react-native";
import React from "react";

const TextComponent = ({ children }: { children: React.ReactNode }) => {
  return <Text>TextComponent</Text>;
};

export default TextComponent;
